У меня 10 номер в списке группы, поэтому я выполняла задание 5.
В репозитории есть следующие файлы:
  blacklist - файл с черным списком серверов. Он хранится в /etc/, c ним сверяется мой сервер.
  serv.sh -  скрипт, выполняющий, собственно, работу сервера: записывает список работающих служб в файл и сверяет его с черным списком. 
В случае найденных совпадений сервис закрывает эту службу.
  BlacklistServ.service - сам сервис
  Backlist.te - модуль безопасности системы
  Blacklist.8.gz - man-документация для сервера
Сервис работает как системная служба, выполняет свою функцию раз в минуту, ведет свой журнал (можно посмотреть с помощью journalctl), при получении сигнала 
USR1 немедленно выполняет свою основную функцию. Разработана man-документация и политика безопасности SELinux.
